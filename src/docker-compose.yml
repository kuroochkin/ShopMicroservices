version: '3.4'

services:
  catalogdb:
    image: postgres
    
  basketdb:
    image: postgres
    
  catalog.api:
    container_name: ${DOCKER_REGISTRY-}catalogapi
    image: catalog.api
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:
    
